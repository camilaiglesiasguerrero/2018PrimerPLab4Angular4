import { BackgroundSexoDirective } from './background-sexo.directive';

describe('BackgroundSexoDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundSexoDirective();
    expect(directive).toBeTruthy();
  });
});
